using Prop;

TestProp test = new TestProp(); // Crée un objet test de type TestProp

//test.data = 10; // Accès à la donnée (membre) data de l'objet test

test.Nb1 = 2;
test.Nb2 = 0;
Console.WriteLine(test.Quotient()); // Affiche la donnée (membre) data de l'objet test